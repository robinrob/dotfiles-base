typeset url
result=`ls -d .git 2> /dev/null`
if [ "$result" ]
then
  url=$(gh_url)
  green "Repo found: $(gh_url)"
else
	url="https://github.com/robinrob"
fi

green "Opening $url ..."
open -a $BROWSER $url

