typeset url
result=$(ls -d .git 2> /dev/null)
if [ "$result" ]
then
  url=$(repo_url)
  green "Repo found: $(repo_url)"
else
	url="https://github.com/robinrob"
fi

green "Opening $url ..."
open -a $BROWSER $url

