local +r -a Funcs

if [[ $@# > 0 ]]
then
  print "$(green Un-functioning:) $(yellow $@)"
  Funcs=($*)
else
  green "Unfunctionning all functions"
  Funcs=($FUNCS_HOME/*(:t))
fi

for func in $Funcs
do
  unfunction $func 2> /dev/null
done
