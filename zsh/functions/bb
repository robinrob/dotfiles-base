typeset url
result=`ls -d .git 2> /dev/null`
if [ "$result" ]
then
  url=$(bb_url)
  green "Repo found: $(bb_url)"
else
	url="https://bitbucket.org/robinrob"
fi

green "Opening $url ..."
open -a $BROWSER $url

