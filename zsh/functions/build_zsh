local +r FunctionName=$1
local +r ScriptPath=$2

rm -f $ScriptPath
touch $ScriptPath

local +r Script="$(expand_function $FunctionName)\n$(which create_color_functions)\ncreate_color_functions"

print $Script >> $ScriptPath

