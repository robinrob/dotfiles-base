if [[ -n $(is_remote_git) ]]
then
  if [[ $(git_host) == "github.com" ]]
  then
    Location="tree/$(git_branch)"
  elif [[ $(git_host) == "bitbucket.org" ]]
  then
    Location="src/?at=$(git_branch)"
  fi

  print "https://$(git_host)/$(git_owner)/$(git_project)/$Location"
fi
