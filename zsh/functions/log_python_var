local +r Variables=($*)
local +r Strings=()


for variable in $Variables
do
  Strings=($Strings "${String}print '$variable: {var}'.format(var=$variable)")
done

join $Strings '\n' | pbcopy && pbpaste

