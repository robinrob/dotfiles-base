IFS=' '

# case $(basename $(pwd)) in
  # robinrob.github.io-dev) TASK="jekyll:save" ;;
  # *) TASK="base:save" ;;
# esac

rake_do base:save "$*"

# if [[ $(is_in_set 'bitbucket github origin live' $*[-1]) == "yes" ]]
# then
#   rake_do $TASK $*[1,-2] $*[-1]
# else
# fi

