case $#@ in
  1)
    egrep -ri $1 * ;;
  2)
    find $2 -type f -exec printf {}: \; -exec egrep -H $1 {} \; ;;
  3)
    find $2 -maxdepth $3 -type f -exec egrep -H $1 {} \; ;;
  *)
    red "egrep_recursive <regex> <location> <depth>" ;;
esac
