TASK=$1

if [ -f Rakefile ]
then
print "$(green "Using Rakefile: ")$(yellow $(/usr/local/bin/gls $PWD/Rakefile))"

shift

if [[ -n $* ]]
then
cmd='rake '$TASK'['''$(join ', ' $*)''']'
else
cmd="rake $TASK"
fi

eval $cmd
green $cmd
else
red "No Rakefile!"
# rake -f $RAKEFILE_HOME/Rakefile save
fi

