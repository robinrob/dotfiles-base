TASK=$1


if [ -f $RAKEFILE ]
then
  print "$(green "Using Rakefile: ")$(yellow)$RAKEFILE"

  shift

  if [[ -n $* ]]
  then
    cmd='rake --rakefile $RAKEFILE --libdir $RAKEFILELIB '$TASK'['''$(join ', ' $*)''']'
	else
		cmd="rake $TASK"
	fi

  eval $cmd
  green $cmd
else
	red "No Rakefile!"
fi

