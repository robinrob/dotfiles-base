local +r SedExpression=$1

if [[ -n $2 ]]
then
  local +r FindExpression=$2
else
  local +r FindExpression='*'
fi


IFS="
"
for file in $(gfind . -name "$FindExpression" -type f)
do
  gsed -i $SedExpression $file
done
