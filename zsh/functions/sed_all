SED_EXPRESSION=$1
FIND_EXPRESSION=$2

files=$(gfind . -type f -name "$FIND_EXPRESSION" | xargs)
print "files: $files"
files=(${(s/ /)files})

for file in $files
do
  gsed -i $SED_EXPRESSION $file
done

