Pattern=$1
( [[ -n $2 ]] && Where=$2 ) || Where='.'
MaxDepth=$3

if (( $MaxDepth > 0 ))
then
  cmd="gfind $Where -maxdepth $MaxDepth -iname '*$Pattern*'"
else
  cmd="gfind $Where -iname '*$Pattern*'"
fi

green $cmd
eval $cmd
