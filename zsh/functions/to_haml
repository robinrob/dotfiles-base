rvm use 2.1.4 > /dev/null

if (( $# == 0 ))
then
  find . -maxdepth 1 -name \*.html -print | sed 'p;s/.html$/.haml/' | xargs -n2 html2haml --no-erb --html-attributes
else
  for file in $*
  do
    html2haml --no-erb $file > $(name $file).haml
  done
fi
