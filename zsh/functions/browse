local +r Browser=$1
local +r Url=$2

if $(is_not_empty $Url)
then
  if $(is_http_url $Url)
  then
    Url=$Url
  else
    Url="https://www.google.com/#q=$(urlencode $*[2,-1])&safe=active"
  fi
fi

eval "$Browser '$Url'"
