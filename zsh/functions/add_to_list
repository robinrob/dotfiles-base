LIST=$1
shift

if (( $#* == 2 ))
then
  NAME=$1
  VALUE=$2
  ITEM="$NAME::$VALUE"
  print "$(yellow)$NAME $(green)added to $(yellow)$LIST $(green)as $(yellow)$VALUE$(default)"
else
  VALUE=$1
  ITEM=$VALUE
  print "$(yellow)$VALUE $(green)added to $(yellow)$LIST$(default)"
fi

if [[ -n "$ITEM" ]]
then
  print "$ITEM" >> $LISTS_HOME/${LIST}.txt
fi

