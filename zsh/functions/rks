case $(basename $(pwd)) in
  robinrob.github.io-dev) TASK="jekyll:save" ;;
  *) TASK="base:save" ;;
esac


if [[ $*[-1] == "bitbucket" || $*[-1] == "github" || $*[-1] == "origin" ]]
then
  rake_do $TASK "$*[1,-2]" $*[-1]
else
  rake_do $TASK "$*"
fi

